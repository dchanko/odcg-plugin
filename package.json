{
  "name": "odcg-calculator",
  "version": "1.0.0",
  "description": "Embeddable client-side calculator widget for WordPress",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:docs": "vite build && node -e \"require('fs').copyFileSync('dist/odcg.min.js','docs/odcg.min.js')\"",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  }
}
